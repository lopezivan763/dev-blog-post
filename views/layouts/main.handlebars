<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fund My Project!</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body style="background-color: pink;">
  <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-between align-center p-2" style="background-color: rgb(37, 161, 214);">
      <h1>
        <a href="/" style="border: solid; background-color: pink;">The Dev Blog</a>
      </h1>
      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/profile" style="color: azure;">profile</a> |
        <button class="no-button" id="logout" style="color: azure;">logout</button>
        {{else}}
        <a href="/login" style="color: azure;">login</a>
        {{/if}}
      </nav>
    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}

  <footer>

    </footer>
</body>

</html>
